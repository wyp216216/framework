import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

// 要设置的初始属性值
const state = {
}

const getters = {
}

const mutations = {
}


const store = new Vuex.Store({
  state,
  getters,
  mutations,
})

export default store;
